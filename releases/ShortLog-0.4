yellow (15):
      releasing zak (property management system) version 0.3
      restoring today link on tableau (planning)
      Heavy bug: massive mod reservation on ModOccupancy()
      recompiling js
      unknonw flag
      unixDate() on tableau, today link working again
      Invoice seed now editable
      Enter reservation details now working when inserting     new reservations
      preparing tooltip for reservations planning
      Adding icon to "Add reservation on the fly"
      checkOccupancy() debug (same dfrom collision)     and flyReservation()
      removing useless deps
      Better AddRes img and reservation tooltips
      Reservation tooltips
      pre release 0.4, preparing
